export
fn test_vpermq512(reg u64 tmp)
{
  reg u256 ff0 ff1 idx0;

  ff1 = #set0_256();
  idx0 = [:u256 tmp];

  ff0 = #VPERMQXVAR_256(idx0, ff1);
  [:u256 tmp] = ff0;

  reg u512 f0 f1 idx;

  f1 = #set0_512();
  idx = [:u512 tmp];

  f0 = #VPERMQXVAR_512(idx, f1);
  [:u512 tmp] = f0;
}
